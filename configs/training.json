{
  "metadata": {
    "name": "AWARE-NET Stage 00 Training Configuration",
    "description": "Simplified training config for baseline model",
    "version": "1.0.0"
  },

  "experiment": {
    "name": "stage01_indistribution_high_performance",
    "description": "Stage 01: In-distribution high-performance filter with EfficientNetV2-B0, 4 balanced datasets, optimized for speed and high recall",
    "tags": ["stage01", "in-distribution", "high-performance", "efficientnet", "multi-dataset", "balanced"]
  },

  "model": {
    "name": "tf_efficientnetv2_b0",
    "pretrained": true,
    "dropout_rate": 0.2,
    "freeze_backbone": false
  },

  "training": {
    "batch_size": 128,
    "epochs": 10,
    "learning_rate": 0.003,
    "weight_decay": 0.0001,
    "optimizer": "adamw",
    "scheduler": "cosine",
    "num_workers": 16,
    "mixed_precision": true,
    "early_stopping_patience": 10,
    "gradient_clipping": 1.0,
    "warmup_epochs": 3,
    "use_class_weights": false,
    "comment": "Stage 01 RTX 5090 optimized: batch_size=128, epochs=10 for quick validation test, lr=0.003, workers=16, FIXED checkpoint saving with improved floating point comparison"
  },

  "data": {
    "dataset_config": "configs/datasets.json",
    "dataset_name": "celebdf_v2",
    "dataset_mode": "balanced",
    "multi_dataset": true,
    "use_dataset_weights": true,
    "strict_batch_balance": false,
    "image_size": 256,
    "augmentation": true,
    "subset_ratio": 1.0,
    "comment": "dataset_mode: 'balanced' (50/50 class balance). use_dataset_weights: equal 25% per dataset. strict_batch_balance: enforce exact 64-sample batches"
  },

  "monitoring": {
    "log_interval": 100,
    "save_interval": 5,
    "eval_interval": 1,
    "save_best_only": true,
    "monitor_metric": "val_auc"
  },

  "paths": {
    "models_dir": "models/stage_00",
    "results_dir": "results/stage_00",
    "logs_dir": "logs/stage_00",
    "experiments_dir": "experiments"
  },

  "targets": {
    "min_auc": 0.95,
    "min_f1": 0.93,
    "max_inference_time_ms": 50,
    "max_memory_usage_gb": 2,
    "min_recall": 0.99,
    "max_false_negative_rate": 0.01,
    "min_filter_rate": 0.80
  }
}