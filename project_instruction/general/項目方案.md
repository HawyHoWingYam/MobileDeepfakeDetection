# 面向移動端防詐騙應用的級聯式異構集成 Deepfake 檢測模型研究

## 📋 項目概述

本研究旨在設計並實現一個專為移動端設計的高效、高精度Deepfake檢測應用原型，核心目標是防範基於面部替換（Face Swap）技術的影片詐騙。為兼顧移動端嚴苛的性能限制與高檢測精度的雙重需求，本研究提出並探索一種創新的混合架構，該架構深度融合了動態模型級聯（Dynamic Model Cascade）與基於嵌入向量堆疊的異構模型集成（Embedding-based Stacking Ensemble）。

### 🎯 項目願景
本研究提出一個創新的統一混合架構，旨在從被動地「檢測偽造痕跡」轉向主動地「建模真實性」，從而在根本上應對未知威脅。該架構的核心思想是以一個高效的動態兩階段級聯系統為工程骨架，深度融合下一代檢測模型所需的三大理論支柱：泛化性（Generalization）、穩健性（Robustness）與適應性（Adaptability）。

### 🏗️ 技術創新點

## 📊 項目狀態統一報告

**生成時間**: 2025-09-23
**報告版本**: v2.0 (修正後實際狀況)
**評估基準**: 實際代碼實現 + 功能完整性驗證

### 📊 整體項目狀態概覽

#### 核心指標
- **總體完成度**: 27% (修正後準確評估)
- **架構完成階段**: 3/10 (Stage 00, 01, 02)
- **完全實施階段**: 0/10 (所有階段都有關鍵缺失)
- **可用於實際訓練**: Stage 00 (部分), Stage 01-02 (架構only)

#### 當前項目優勢
- ✅ **文檔整合完成**: 35 → 18個文檔 (48%精簡)
- ✅ **項目結構統一**: tools/, configs/, 10-stage架構
- ✅ **配置管理完善**: 按階段和功能分類的配置文件
- ✅ **基礎架構穩固**: 環境管理、數據管道、工具庫完整

#### 關鍵風險警示 ⚠️
1. **文檔-實施不一致性**: 多處文檔錯誤聲稱"100% COMPLETE"
2. **訓練腳本缺失**: Stage 01-02 無法執行實際訓練
3. **性能基線未建立**: Stage 00 基線訓練未執行
4. **階段驗證缺失**: 無實際的stage-gate評估

### 🎯 各階段詳細狀態

#### Stage 00: 基礎設施建設 - 83% ✅ 基本完成
**狀態**: 可用於基線訓練，但缺少API文檔

| 組件 | 狀態 | 完成度 | 說明 |
|------|------|--------|------|
| 環境配置 | ✅ 完成 | 100% | Conda環境 + GPU支持 |
| 數據管理 | ✅ 完成 | 100% | CelebDF-v2配置完整 |
| 工具函數庫 | ✅ 完成 | 100% | 評估、可視化、實驗工具 |
| 基線模型架構 | ✅ 完成 | 100% | EfficientNetV2-B3實現 |
| **基線訓練** | ❌ 未執行 | 0% | **關鍵缺失** |
| API文檔 | ❌ 缺失 | 0% | **影響可用性** |

**立即行動**: 執行CelebDF-v2基線訓練，建立性能基準

#### Stage 01: SupCon快速過濾器 - 90% 🔄 架構完成
**狀態**: 架構完整，但無法訓練驗證

| 組件 | 狀態 | 完成度 | 說明 |
|------|------|--------|------|
| SupCon損失函數 | ✅ 完成 | 100% | 數值穩定性已驗證 |
| MobileNetV4模型 | ✅ 完成 | 100% | 包含投影頭 |
| 溫度校準 | ✅ 完成 | 100% | 完整校準框架 |
| 評估框架 | ✅ 完成 | 100% | 綜合評估系統 |
| **訓練腳本** | ❌ 缺失 | 0% | **阻塞性缺失** |
| 單元測試 | ❌ 缺失 | 0% | SupCon核心組件無測試 |

**立即行動**: 創建`train_stage1_supcon.py`，執行SupCon vs BCE對比

#### Stage 02: 異構專家模型 - 85% 🔄 架構完成
**狀態**: 複雜架構完成，但缺少訓練和驗證組件

| 組件 | 狀態 | 完成度 | 說明 |
|------|------|--------|------|
| 空間專家 | ✅ 完成 | 100% | EfficientNetV2-B3 + Focal Loss |
| GenConViT專家 | ✅ 完成 | 100% | 雙變體 + 多尺度融合 |
| 互補性分析 | ✅ 完成 | 100% | 完整的融合系統 |
| 診斷工具 | ✅ 完成 | 100% | 系統健康監控 |
| **訓練腳本** | ❌ 缺失 | 0% | **阻塞性缺失** |
| 實際驗證 | ❌ 未執行 | 0% | 無性能驗證 |

**立即行動**: 創建訓練腳本，執行專家模型訓練

#### Stage 03-09: 高級階段 - 0% ⏳ 未開始
**狀態**: Stage 03集成接口已就緒，等待前置階段完成

### 🚨 關鍵問題分析

#### 1. 文檔-實施不一致問題
**發現**:
- IMPLEMENTATION_SUMMARY.md錯誤聲稱Stage 02 "100% COMPLETE"
- 多個文檔標記任務"訓練就緒"但缺必需腳本
- 實際完成度被高估約30-40%

**影響**: 可能導致資源錯配、進度誤判、學術可信度下降

#### 2. 訓練腳本系統性缺失
**現狀**:
- Stage 01: `train_stage1_supcon.py` 不存在
- Stage 02: 兩個專家訓練腳本未創建
- 無法執行實際的模型訓練和性能驗證

**影響**: 阻塞所有後續階段，無法建立性能基線

#### 3. 基線性能基準缺失
**問題**: Stage 00基線訓練未執行
**後果**:
- 無法評估後續創新的"淨增益"
- 缺少學術對比基準
- Stage-gate評估無依據

### 📋 優先修復計劃

#### Phase 1: 立即修復 (本週)
1. **執行Stage 00基線訓練** - 使用CelebDF-v2數據集
2. **創建Stage 01訓練腳本** - 實現SupCon vs BCE對比
3. **創建Stage 02訓練腳本** - 兩個專家模型訓練
4. **建立可信狀態管理** - 統一實施狀態追蹤

#### Phase 2: 驗證完善 (下週)
1. **執行10-epoch對比實驗** - SupCon vs BCE性能基準
2. **完成Stage-gate評估** - Stage 01通過標準
3. **生成性能基線報告** - 學術級評估文檔
4. **建立持續集成** - 自動化測試和驗證

### 🎯 修正後的里程碑規劃

#### 近期目標 (2週內)
- [ ] Stage 00: 基線訓練完成 (AUC ≥ 0.85)
- [ ] Stage 01: SupCon vs BCE對比完成 (+3% AUC提升)
- [ ] Stage 02: 專家模型訓練完成
- [ ] 建立統一的項目狀態管理系統

#### 中期目標 (1個月內)
- [ ] Stage 01-02: 完整的stage-gate評估通過
- [ ] Stage 03: 時序建模專家開發啟動
- [ ] 建立完整的性能追蹤體系
- [ ] 準備學術發表材料

### 📊 資源分配建議

#### 開發優先級
1. **最高優先級**: 基線訓練 + 性能基準建立
2. **高優先級**: Stage 01-02訓練腳本創建
3. **中等優先級**: Stage 03時序建模開發
4. **低優先級**: 高級階段(Stage 04+)規劃

#### 技術債務償還
- 補全單元測試覆蓋 (Stage 01 SupCon組件)
- 完善API文檔 (Stage 00工具庫)
- 建立自動化CI/CD管道
- 統一代碼風格和文檔格式

---

## 📖 配置管理與模型存儲指南

### 🚀 快速開始訓練

#### 1. 使用原始數據集訓練（不平衡，存在路徑洩露）
```bash
python src/stage_00/train_baseline.py --dataset original_imbalanced --training original_imbalanced
```

#### 2. 使用清潔平衡數據集訓練（推薦）
```bash
python src/stage_00/train_baseline.py --dataset balanced_clean --training balanced_clean
```

#### 3. 快速3-epoch測試
```bash
python src/stage_00/train_baseline.py --dataset original_imbalanced --training quick_test
```

### 📊 數據集配置選項

#### `celebdf_v2_original`
- **407,712樣本**（59,557真實 + 348,155偽造）
- **5.85:1不平衡**（自然CelebDF-v2分佈）
- **存在路徑洩露**（會顯示不現實的性能）
- **主體級清潔拆分**（零重疊）

#### `celebdf_v2_balanced_anonymized`
- **119,114樣本**（59,557真實 + 59,557偽造）
- **完美50/50平衡**
- **零路徑洩露**（匿名化文件名）
- **主體級清潔拆分**（零重疊）

### 🎯 訓練配置配置文件

#### `original_imbalanced`
- 用於原始數據集結構測試
- 由於路徑洩露預期不現實的性能
- pos_weight = 0.190用於類別不平衡
- **使用案例**：調試和初始驗證

#### `balanced_clean`
- 用於現實的學術訓練
- 預期AUC：0.75-0.85，F1：0.70-0.82
- pos_weight = 1.0（平衡數據）
- **使用案例**：主要訓練配置

#### `quick_test`
- 僅3個epoch用於驗證
- 快速設置測試
- **使用案例**：環境驗證和快速測試

### 📝 預期性能基準

| 數據集 | 配置 | 預期AUC | 預期F1 | 註釋 |
|--------|------|---------|--------|------|
| 原始 | original_imbalanced | 0.95-0.999 | 0.90-0.99 | 路徑洩露誇大指標 |
| 平衡 | balanced_clean | 0.75-0.85 | 0.70-0.82 | 現實性能 |
| 任何 | quick_test | 可變 | 可變 | 僅測試，不用於基準測試 |

### 📁 模型存儲目錄結構

```
models/
├── stage_00/                    # 基線模型和基礎設施
│   ├── baseline_efficientnet_b3.pth
│   ├── checkpoints/
│   └── metadata/
├── stage_01/                    # SupCon快速過濾模型
│   ├── supcon_filter.pth
│   ├── mobilenetv4_supcon.pth
│   └── temperature_scaling_params.pkl
├── stage_02/                    # 異構專家模型
│   ├── spatial_expert_efficientnetv2.pth
│   ├── genconvit_expert.pth
│   └── fusion_system.pth
├── deployment/                 # 生產就緒模型
│   ├── aware_net_mobile.onnx
│   └── quantized/
└── experimental/               # 研究和消融模型
```

### 💾 模型存儲工作流程

#### 訓練期間
```python
# 示例：Stage 0基線訓練
python src/stage_00/train_baseline.py --dataset balanced_clean

# 自動保存到：
# models/stage_00/baseline_efficientnet_b3.pth
# models/stage_00/checkpoints/epoch_*.pth
# models/stage_00/metadata/training_config.json
```

#### 推理時
```python
import torch
from src.stage_00.baseline_model import EfficientNetBaseline

# 加載訓練好的基線模型
model = EfficientNetBaseline(num_classes=1)
model.load_state_dict(torch.load('models/stage_00/baseline_efficientnet_b3.pth'))
```

### 🔧 配置文件結構

#### 數據集配置結構
```json
{
  "datasets": {
    "dataset_name": {
      "type": "image_folder",
      "path": "/path/to/dataset",
      "split_ratios": [0.7, 0.15, 0.15],
      "preprocessing": {
        "resize": [256, 256],
        "normalize": "imagenet"
      }
    }
  },
  "default_dataset": "dataset_name"
}
```

#### 訓練配置結構
```json
{
  "training_configs": {
    "config_name": {
      "model": {
        "architecture": "efficientnetv2_rw_s",
        "num_classes": 1,
        "pretrained": true
      },
      "optimizer": {
        "type": "AdamW",
        "lr": 1e-3,
        "weight_decay": 1e-4
      },
      "training": {
        "epochs": 50,
        "batch_size": 32,
        "early_stopping": {
          "patience": 10,
          "min_delta": 0.001
        }
      }
    }
  },
  "default_config": "config_name"
}
```

### 🛠️ 環境配置

#### Conda環境（`environment.yml`）
```yaml
name: aware_net_rtx50
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pytorch>=2.1
  - torchvision
  - pytorch-cuda=11.8
  - pip
  - pip:
    - timm
    - albumentations
    - opencv-python
    - scikit-learn
    - lightgbm
```

### 📋 文件格式標準

| 格式 | 用例 | 示例 |
|------|------|------|
| `.pth` | PyTorch原生權重 | `baseline_efficientnet_b3.pth` |
| `.onnx` | 跨平台部署 | `aware_net_mobile.onnx` |
| `.pkl` | Scikit-learn模型 | `lightgbm_metamodel.pkl` |
| `.json` | 模型元數據和配置 | `training_config.json` |

### 🚀 部署模型管道

#### 模型轉換工作流程
```bash
# 1. 訓練PyTorch模型
python src/stage_00/train_baseline.py --dataset balanced_clean

# 2. 轉換為ONNX用於部署
python tools/deployment/convert_to_onnx.py \
    --model models/stage_00/baseline_efficientnet_b3.pth \
    --output models/deployment/baseline_model.onnx \
    --input-size 3 256 256

# 3. 量化用於移動部署
python tools/deployment/quantize_model.py \
    --input models/deployment/baseline_model.onnx \
    --output models/deployment/quantized/baseline_int8.onnx \
    --precision int8
```

### ⚠️ 當前狀態

#### 可用模型
- ⚠️ **佔位符**：目錄結構存在但尚無訓練模型
- 🔄 **進行中**：Stage 00基線訓練管道就緒
- 📋 **計劃中**：訓練完成後模型將自動保存

#### 缺失組件
- [ ] **Stage 01**：SupCon過濾模型（等待訓練腳本完成）
- [ ] **Stage 02**：專家模型（等待訓練腳本完成）
- [ ] **部署**：ONNX轉換管道（等待模型完成）

---

### 🏗️ 技術創新點
1. **範式轉換**：從被動檢測偽造痕跡轉向主動建模真實性
2. **三支柱統一架構**：泛化性+穩健性+適應性的有機融合
3. **異構時空融合**：空間+生成+時序專家系統
4. **動態級聯智能分配**：計算資源的智能分配策略

---

## 摘要

本研究旨在設計並實現一個專為移動端設計的高效、高精度Deepfake檢測應用原型，核心
目標是防範基於面部替換（Face Swap）技術的影片詐騙。為兼顧移動端嚴苛的性能限制與
高檢測精度的雙重需求，本研究提出並探索一種創新的混合架構，該架構深度融合了動態
模型級聯（ **Dynamic Model Cascade** ）與基於嵌入向量堆疊的異構模型集成（
**Embedding-based Stacking Ensemble** ）。系統將以離線處理模式（先保存後檢測）為主
要應用場景，並針對主流中端移動設備進行性能優化與部署。本研究的預期貢獻在於提供
一套完整的、從模型設計到移動端部署、兼具高精度與高效率的Deepfake檢測解決方案，
並通過一套綜合評估體系驗證其有效性。

## 🎯 研究背景與問題定義

### 1.1 Deepfake的「軍備競賽」與檢測器的脆弱性
隨著生成式AI技術的飛速發展，特別是GANs和擴散模型的成熟，Deepfake已從學術探索演變為一場持續升級的技術競賽。偽造影片的逼真度呈指數級增長，而被用於金融詐騙、假新聞傳播和個人名譽攻擊的案例日益增多。然而，當前的檢測技術卻陷入了被動追趕的困境。其核心脆弱性在於，現有模型極度依賴於識別訓練數據中特定生成器留下的獨特「指紋」，例如特定的上採樣偽影、獨特的頻譜噪點或微弱的紋理不一致性。

這種方法的致命缺陷是過擬合於偽造品的表象。一旦攻擊者採用新的生成架構，或對偽造影片進行簡單的後處理（如壓縮、縮放、添加噪點），這些脆弱的「指紋」便會消失或改變，導致檢測模型性能急遽下降。這意味著，現有檢測器本質上是一個封閉集合（closed-set）問題的解決方案，卻被錯誤地應用於一個開放集合（open-set）的現實世界。

### 1.2 定義下一代檢測器的三大支柱
為打破這種被動局面，一個真正有效的、面向未來的檢測框架，必須在設計之初就確立三個相互依存、缺一不可的核心支柱：

**泛化性 (Generalization)**：這是檢測器的基石。它要求模型能夠檢測出由未知或新型偽造技術生成的內容。缺乏泛化性，我們將永遠落後於攻擊者一步，只能在新的攻擊方式造成大規模危害後才開始被動地收集數據、訓練新模型，陷入永無止境的「補丁式」防禦。

**穩健性 (Robustness)**：如果說泛化性是模型的「智力」，穩健性就是其「體魄」。一個缺乏穩健性的模型，即使泛化能力再強，也如同一座「紙牌屋」，輕易就會被惡意用戶精心設計的對抗性攻擊所欺騙。這些攻擊通過在偽造影片上添加人眼難以察覺的微小擾動，就能讓模型「指鹿為馬」。

**適應性 (Adaptability)**：偽造技術的演進永不停歇，這要求檢測器必須是一個能夠自我進化的生命體。適應性要求模型能在新的偽造技術出現時，快速、高效地學習其特徵，同時避免遺忘對舊有技術的檢測能力。這涉及到持續學習領域核心的「穩定性-可塑性困境」。

### 1.3 研究範圍與約束
- **核心問題**：如何設計一個統一框架，將泛化性、穩健性、適應性這三個時常相互衝突的目標，有機地融入一個計算資源受限但性能要求極高的高效移動端級聯檢測系統中？
- **應用場景**：專注於移動端離線影片檢測，防範基於面部替換（Face Swap）技術的影片詐騙。
- **學術導向**：以算法創新、理論驗證和頂級會議論文發表為核心目標，強調方法的可解釋性與實驗的可重現性。

```
● 研究範圍：本研究將嚴格限定在對 Face Swap 類型偽造影片的檢測，暫不涵蓋表情驅
動、音訊偽造等其他類型。
● 應用場景：核心應用為離線檢測模式。用戶可將來自社交軟體（如Zoom, Teams,
WhatsApp, WeChat等）的螢幕錄影，或其他可疑影片文件保存至本地，由本應用進行
深度分析，適用於事後取證、內容審核等場景。
```
**2.** 核心技術路徑

**2.1** 動態兩階段級聯架構 **(Dynamic Two-Stage Cascade)**

為解決精度與效率的矛盾，本方案採用級聯架構，將計算資源智能地分配給最需要的樣本。

```
● 第一階段 ( 快速過濾 ) ：使用一個計算開銷極低的輕量級模型
MobileNetV4-Hybrid-Medium 。此階段的目標是快速、準確地識別並過濾掉絕大部
分「簡單樣本」。
● 第二階段 ( 精準分析 ) ：僅對第一階段無法確定的「困難樣本」，啟用一個強大的異構集
成模型 (EfficientNetV2-B3 + GenConViT) 進行最終判決。
● 動態閾值策略 (Dynamic Threshold Strategy)：
為提升級聯策略的適應性，將採用基於影片整體不確定性的自適應閾值。
```
1. 概率校準：首先，對MobileNetV4模型在驗證集上進行溫度縮放（ **Temperature**
    **Scaling** ），使其輸出的置信度能更準確地反映真實概率。
2. 自適應調整：對輸入影片進行初次掃描，計算所有幀置信度的標準差。若標準差高
    （影片難度大），則擴大「困難樣本」的置信度區間（如 [0.2, 0.8]）；若標準差低（影片
    難度小），則收緊區間（如 [0.4, 0.6]），從而實現動態調整。


```
● 風險評估與控制 (Risk Assessment & Control)：
為嚴格控制第一階段的假陰性風險（即漏掉偽造樣本），將採用非對稱性保守閾值策
略。
```
1. 策略定義：僅當置信度極高（如 **> 0.98** ） 時，才判定為「簡單真實樣本」並放行。而所
    有置信度在 [0.1, 0.98] 之間的樣本，均視為「困難樣本」送入第二階段。
2. 評估指標：引入**「第一階段洩漏率 (Stage-1 Leakage Rate)」** 作為關鍵風險指
    標，用以量化在測試集中，應為偽造的樣本被第一階段錯誤放過的比例。目標是將
    此比率降至最低。

**2.2** 基於堆疊的異構集成 **(Stacking-based Heterogeneous Ensemble)**

```
● 基礎模型與協同作用 (Base Models & Synergy) ：
○ EfficientNetV2-B3 ：作為頂級的卷積神經網絡（CNN），預期其更擅長捕捉由偽造
過程引入的局部紋理異常、頻域噪點和邊緣不一致性。
○ GenConViT ：作為卷積-Transformer混合模型，預期其能利用自註意力機制，有效
建模影片幀內乃至幀間的全局語義不一致性、光照與陰影的異常關係等宏觀破綻。
● 融合策略：嵌入向量堆疊 (Fusion Strategy: Embedding Vector Stacking)：
採用堆疊（Stacking）方法，並以梯度提升樹（如LightGBM） 作為元模型。
```
1. 輸入：元模型的輸入並非單純的置信度分數，而是從兩個基礎模型分類頭之前的嵌
    入層（ **Embedding Layer** ） 提取的特徵向量。
2. 對齊與拼接：通過獨立的全連接層將兩個不同維度的嵌入向量投影到統一維度（如
    256 維），然後進行拼接，形成一個信息更豐富的、更高維的輸入特徵。
3. 訓練流程：為防止數據洩漏，將採用嚴格的 **K-Fold** 交叉驗證策略來生成用於訓練
    元模型的數據集。

**2.3** 移動端部署與優化 **(Mobile Deployment & Optimization)**

```
● 性能目標：本研究旨在將模型部署於搭載2022-2025年間發布的中端晶片組（如高通驍
龍 7 系、聯發科天璣 8000 系）的智能手機上，並設定具體的、可量化的延遲與精度目
標。
● 精度優先的優化策略 (Accuracy-First Optimization Strategy)：
為在模型壓縮過程中最大程度地保留精度，將採用量化感知訓練（
Quantization-Aware Training, QAT） 與知識蒸餾（Knowledge Distillation, KD） 相結合
的先進策略。
```
1. 教師模型：將PC端訓練的、未經量化的高精度完整集成模型作為「教師」。
2. 學生模型：將部署於移動端的量化版模型作為「學生」。
3. 訓練過程：在對學生模型進行QAT時，不僅使其學習真實標籤，還引入蒸餾損失，
    使其模仿教師模型的輸出，從而繼承教師模型的泛化能力，最大限度地減少精度
    損失。

---

## 📊 10階段實施架構

本研究將原有的6階段計劃擴展為更具操作性的10階段實施方案，確保每個階段都有明確的技術目標、學術價值和實現路徑。

### **階段 0：基礎設施建設** (Week 1)
**核心目標**：建立可重現的學術研究環境並建立強力基線模型
- **環境配置**：Docker容器化 + environment.yml (PyTorch 2.1+, CUDA 11.8)
- **數據管理**：dataset_config.py + JSON配置系統 + manifest文件自動生成
- **學術工具庫**：metrics.py (AUC+置信區間), visualization.py (ROC+可靠性圖), experiment_utils.py
- **強力基線模型**：EfficientNetV2-B3 + BCE Loss，目標AUC ≥ 0.88-0.90
- **項目結構**：建立完整的10階段目錄架構，每階段獨立可測試

### **階段 1：SupCon快速過濾器** (Week 2)
**核心創新**：「真實性建模」範式轉換
- **SupCon損失實現**：監督式對比學習替代交叉熵
- **MobileNetV4訓練**：構建「真實性堡壘」的特徵空間
- **溫度縮放校準**：概率可解釋性 + 保守閾值策略
- **學術價值**：範式轉換的理論突破，SupCon在深偽檢測的首次系統應用

### **階段 2：異構專家模型** (Week 3-4)
**核心策略**：專業分工的檢測策略
- **EfficientNetV2-B3**：空間偽影檢測專家
- **GenConViT**：生成結構檢測專家（雙變體：ED + VAE）
- **異構互補性驗證**：確保專家間的能力差異和互補
- **學術價值**：異構專家系統設計，生成式檢測方法創新

### **階段 3：時序建模專家** (Week 5)
**核心技術**：時空檢測維度擴展
- **Temporal Transformer**：跨幀語義一致性分析
- **兩階段訓練**：特徵學習 + 時序建模平衡
- **時序一致性損失**：懲罰相鄰幀預測劇烈變化

### **階段 4：特徵融合系統** (Week 6)
**核心技術**：異構智能協同融合
- **K-Fold特徵生成**：防止數據洩漏的無偏特徵
- **時空特徵融合**：三專家特徵的智能組合
- **LightGBM元模型**：學習專家間協同模式

### **階段 5：SAT對抗訓練** (Week 7)
**核心創新**：攻擊感知學習
- **SAT理論框架**：將攻擊類型和強度作為自監督任務
- **端到端對抗訓練**：整個融合系統的聯合優化
- **穩健性注入**：主動理解攻擊模式的防禦機制

### **階段 6-9：系統整合與優化** (Week 8-12)
- **階段 6**：級聯系統整合與持續學習
- **階段 7**：系統優化與移動端部署
- **階段 8**：綜合評估與學術分析
- **階段 9**：開源發佈與標準化

---

## 🎓 學術貢獻與創新評估

### 理論貢獻層面 ★★★★★
**突破性貢獻**：
- **概念創新**：「真實性建模」vs「偽造檢測」的範式轉換
- **理論基礎**：基於對比學習的流形學習理論
- **影響範圍**：可能影響整個生成內容檢測領域

### 技術創新層面 ★★★★☆
**系統性解決方案**：
- **完整性**：從理論到實現的完整技術棧
- **實用性**：面向移動端部署的現實需求
- **可擴展性**：級聯架構支持未來擴展

### 與技術前沿對比分析
| 技術維度 | 現有前沿方法 | 本項目創新 | 新穎性等級 |
|---------|-------------|-----------|-----------|
| 學習範式 | 監督分類學習 | SupCon真實性建模 | ★★★★★ |
| 防禦機制 | 被動特徵檢測 | 主動對抗性理解 | ★★★★☆ |
| 系統架構 | 單階段檢測器 | 動態級聯系統 | ★★★★☆ |
| 時序建模 | 簡單LSTM/GRU | Transformer時序專家 | ★★★☆☆ |
| 適應機制 | 重新訓練 | 持續學習機制 | ★★★★☆ |

---

## 🔧 技術API參考

### 核心接口
```python
# 基礎專家接口
from src.stage_02.unified_feature_extractor import BaseExpert, ExpertOutput

class BaseExpert(nn.Module):
    def __init__(self, expert_type: ExpertType, config: Dict[str, Any])
    def forward(self, input_tensor: torch.Tensor) -> ExpertOutput
    def get_features(self, input_tensor: torch.Tensor) -> torch.Tensor
    def calibrate_predictions(self, logits: torch.Tensor) -> torch.Tensor
```

### SupCon損失函數
```python
from src.stage_01.supcon_loss import SupConLoss

class SupConLoss(nn.Module):
    def __init__(self, temperature: float = 0.07, contrast_mode: str = 'all')
    def forward(self, features: torch.Tensor, labels: torch.Tensor) -> torch.Tensor
```

### 級聯檢測系統
```python
from src.stage_01.cascade_strategy import CascadeDetector

class CascadeDetector:
    def __init__(self, stage1_model: nn.Module, stage2_model: nn.Module)
    def predict(self, input_data: torch.Tensor) -> Dict[str, Any]
    def set_dynamic_threshold(self, confidence_stats: Dict[str, float])
```

---

**3.** 數據集與評估體系


## 3.1 數據集策略

```
● 訓練集 (Training Set) ：構建一個大規模、高質量的多樣化訓練集，包含： DF40 ,
DFDC , Celeb-DF-v2 , FaceForensics++ 。將嚴格使用這些數據集的官方訓練集（
Training Set）和驗證集（Validation Set）進行模型訓練與調優。
● 測試集 (Test Set) ：為確保評估的公正性和客觀性，測試集將與訓練集完全分離，無任
何交集。測試集將由以下部分組成：
```
1. 跨數據集泛化測試：Celeb-DF-v2 和 FaceForensics++ 的官方獨立測試集。
2. 真實世界場景泛化測試：完整的 **Deepfake-Eval-2024** 數據集，作為檢驗模型在
    複雜現實環境中表現的黃金標準。

**3.2** 綜合評估指標

本研究將建立一套多維度的評估體系，以全面衡量整個級聯系統的性能。

```
● 模型精度指標：在各測試集上計算標準的分類指標，包括 AUC-ROC 、準確率（
Accuracy ）、 F1-Score 。
● 系統級性能指標：
```
1. 系統總精度：評估整個級聯模型從輸入到輸出的端到端準確率。
2. 平均處理延遲：綜合考慮第一階段的快速處理和第二階段的慢速處理，計算處理每
    幀或每段影片的加權平均耗時。
3. 計算資源節約指標：量化對比本級聯方案與直接使用重型集成模型的方案在
    FLOPs或實際延遲上的節約比例。
● 泛化與魯棒性評估：
1. 跨偽造技術測試：在訓練時從DF40中保留部分偽造技術，專門用於測試模型對未
知攻擊的泛化能力。
2. 數據擾動魯棒性測試：對測試集人工加入高強度壓縮、噪點、模糊等擾動，評估模
型性能的下降曲線。
**4.** 預期貢獻
1. 提出並驗證一種動態級聯架構，能根據影片難度自適應調整計算負載，為移動端高效
檢測提供新思路。
2. 實現一個基於嵌入向量堆疊的異構集成模型，證明其在Deepfake檢測任務中相比單一
模型的優越性。
3. 提供一套完整的、精度優先的移動端部署優化流程（ **QAT+KD** ），為高性能AI模型在資
源受限設備上的落地提供實踐參考。
4. 建立一套綜合性、多維度的評估體系，能更全面、公允地衡量Deepfake檢測系統在真
實世界中的潛在表現。


