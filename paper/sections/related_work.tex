Deepfake detection research spans spatial, frequency, and multimodal cues, as well as dataset curation and evaluation protocols~\cite{deepfake_survey}. Approaches range from handcrafted forensic features (e.g., head pose, blink patterns, compression artifacts) to modern CNN/Transformer models trained end\hyp to\hyp end on large corpora. Recent in\hyp the\hyp wild datasets emphasize distribution shift and multilingual, multi\hyp source coverage, motivating cross\hyp dataset generalization as a first\hyp class objective.

For on\hyp device deployment, lightweight architectures and model compression are critical. Mobile CNNs (MobileNet families, ShuffleNet, EfficientNet\hyp Lite) and mixed\hyp precision quantization enable efficient inference under tight compute and memory budgets. Early\hyp exit and cascaded designs trade accuracy for efficiency by exiting when model confidence is high and deferring hard examples. Our work leverages MobileNetV4~\cite{mobilenetv4} for fast Stage~1 filtering and EfficientNetV2~\cite{efficientnetv2} as a Stage~2 expert. For deployment, we adopt TorchScript and PyTorch Mobile~\cite{torchscript,pytorch_mobile} with dynamic quantization, and note potential migration to the \texttt{torchao} stack~\cite{torchao} for per\hyp channel and 4\,bit schemes.

We position our cascade within prior art by emphasizing three aspects: (i) multi\hyp dataset training and difficult\hyp sample mining to improve generalization; (ii) threshold\hyp based escalation tuned explicitly for low false negatives and controlled Stage~2 usage; and (iii) end\hyp to\hyp end mobile integration with artifact export, footprint accounting, and on\hyp device measurement. Unlike single\hyp model detectors, our system exposes designer\hyp visible trade\hyp off dials (thresholds, temperature) that translate directly to operational metrics (escalation rate, CPU budget), making the approach attractive for practical deployments.
